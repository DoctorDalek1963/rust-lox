fun mod(a, b) {
	while (a - b > 0) a = a - b;
	return a;
}

for (var i = 1; i <= 100; i = i + 1) {
	var string = "";

	if (mod(i, 3) == 0) string = string + "Fizz";
	if (mod(i, 5) == 0) string = string + "Buzz";

	if (string == "") print i; else print string;
}
